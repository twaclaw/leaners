// Repository coordinates, used to build "Edit this page" links.
// Leave owner/repo empty to auto-detect from a github.io URL.
export const CONFIG = {
  owner: "",
  repo: "",
  branch: "main",
  contentDir: "content",
};
